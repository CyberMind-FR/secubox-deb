# /etc/nginx/secubox.d/mediaflow.conf
# Installed by secubox-mediaflow package
location /api/v1/mediaflow/ {
    proxy_pass http://unix:/run/secubox/mediaflow.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
