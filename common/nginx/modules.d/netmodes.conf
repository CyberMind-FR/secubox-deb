# /etc/nginx/secubox.d/netmodes.conf
# Installed by secubox-netmodes package
location /api/v1/netmodes/ {
    proxy_pass http://unix:/run/secubox/netmodes.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
