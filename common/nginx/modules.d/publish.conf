# /etc/nginx/secubox.d/publish.conf
# Installed by secubox-publish package
location /api/v1/publish/ {
    proxy_pass http://unix:/run/secubox/publish.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
