# /etc/nginx/secubox.d/wireguard.conf
# Installed by secubox-wireguard package
location /api/v1/wireguard/ {
    proxy_pass http://unix:/run/secubox/wireguard.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
