secubox-auth (1.1.9-1~bookworm1) bookworm; urgency=medium

  * Ne livre plus /usr/share/secubox/www/shared en lien (pendant vers
    ../../secubox-hub/www/shared) : dpkg partage un répertoire, pas un
    lien, et deux paquets porteurs du lien dépaquetés avant secubox-hub se
    heurtaient — profil incomplet, image non produite. Le répertoire reste
    livré par secubox-hub et secubox-sbxui (ref #1504).

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 26 Sep 2026 21:00:00 +0200

secubox-auth (1.1.8-1~bookworm1) bookworm; urgency=medium

  * sessions.json : user_agent sur 300 caractères, 100 coupait avant le nom
    du navigateur (ref #1474).

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 25 Sep 2026 18:40:25 +0200

secubox-auth (1.1.7-1~bookworm1) bookworm; urgency=high

  * SÉCURITÉ (ref #1416, audit #1405 S6) : UMask=0000 → 0002. L'unité créait
    ses fichiers en 0666 et ses dossiers en 0777, inscriptibles par tout
    compte de la machine. 0002 retire seulement l'écriture « autres » ;
    toutes les lectures existantes sont conservées.

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 25 Sep 2026 14:13:17 +0200

secubox-auth (1.1.6-1~bookworm1) bookworm; urgency=medium

  * Événement sessions_coupees : ferme des sessions PRÉCISES par jti (ref
    #1369). Un appareil rattaché à gk2 ouvre ses sessions au nom de gk2 ; le
    révoquer ne doit pas déconnecter gk2 de ses autres appareils.

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 24 Sep 2026 11:06:01 +0200

secubox-auth (1.1.5-1~bookworm1) bookworm; urgency=high

  * Journal d'authentification jamais modifiable par tous : audit.log est
    créé en 0640 et ramené à 0640 s'il était plus large ; toute sauvegarde
    JSON retire l'écriture pour « autres » (ref #1366). Trouvé en 0666 sur
    gk2 : n'importe quel compte pouvait forger ou effacer une connexion.
  * postinst : audit.log, history.json, webhooks.json → 0640 ; o-w sur le
    reste de /var/lib/secubox/auth (sessions.json garde sa lecture).

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 24 Sep 2026 09:31:42 +0200

secubox-auth (1.1.4-1~bookworm1) bookworm; urgency=medium

  * Identifiants dérivés : les hachages hérités (MD5/SHA-1) passent par
    secubox_core.crypto.empreinte.ident() — SHA-256 tronqué, parties séparées
    sans ambiguïté (ref #1288). Aucun n'était une faute de sécurité : ce sont
    des étiquettes, pas des preuves. Mais un évaluateur qui cherche « md5 »
    devait examiner chaque cas pour conclure qu'aucun n'était grave.
  * L'identifiant est calculé À LA CRÉATION puis stocké : les enregistrements
    existants gardent le leur, rien ne casse.
  * Dépendance secubox-core (>= 1.4.3), qui apporte le module.

 -- Gerald KERMA <devel@cybermind.fr>  Mon, 14 Sep 2026 11:27:09 +0200

secubox-auth (1.1.3-1~bookworm1) bookworm; urgency=medium

  * LECTURE GARDEE (#1256). Les routes d'affichage de ce module — status,
    components, access, stats — repondaient sans authentification : c'etait le
    motif « three-fold » du parc, un choix assume, mais une lecture publique
    reste de la reconnaissance offerte. Elles passent sous
    `require_lecture` : jeton, OU mode tableau de bord depuis le LAN si
    l'operateur l'a arme dans /etc/secubox/secubox.conf.
  * Le defaut est FERME : sans `[tableau_de_bord] actif = true`, ces routes
    exigent un jeton, y compris depuis le LAN.
  * Les sondes de sante restent publiques. Les routes deja sous require_jwt ne
    changent pas — le mode ne les ouvre jamais.

 -- Gerald Kerma <devel@cybermind.fr>  Thu, 10 Sep 2026 23:55:00 +0200

secubox-auth (1.1.2-1~bookworm1) bookworm; urgency=medium

  * L'unite retire sa socket perimee via `ExecStartPre=+/bin/rm`.
    /run/secubox porte le bit collant : on n'y delie que ce qu'on possede,
    et une socket laissee par un autre utilisateur rend le demarrage
    impossible — le service echoue, redemarre, echoue encore, en silence.
  * `RuntimeDirectory=secubox` retire : systemd efface le contenu d'un
    RuntimeDirectory au redemarrage, et ce repertoire est partage par ~95
    unites. Une seule qui le declare emporte les sockets des autres.

 -- Gerald KERMA <devel@cybermind.fr>  Sun, 16 Aug 2026 10:35:59 +0200

secubox-auth (1.1.1-1~bookworm1) bookworm; urgency=high

  * SECURITY: sessions.json was drwxrwxrwx / rw-rw-rw- on gk2 — any local
    account could append a row and forge a live session, which since #942 the
    whole fleet trusts. postinst now owns it secubox:secubox and closes write
    to the owner. Read is left open on purpose: 12 services run under their own
    user and several call require_jwt, so 0640 would trade a forgery hole for
    an outage. (ref #942)

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 31 Jul 2026 19:30:00 +0200

secubox-auth (1.1.0-1~bookworm1) bookworm; urgency=medium

  * Phase 2: Requires=secubox-core.service -> Wants= on this module's unit
    (remove the hard cascade-stop when secubox-core restarts/fails).

 -- Gerald KERMA <devel@cybermind.fr>  Sun, 19 Jul 2026 12:00:00 +0200

secubox-auth (1.0.4-1~bookworm1) bookworm; urgency=medium

  * feat: admin-TOTP requirement is now toggleable from the webui and defaults
    OFF so a node stays reachable (was: implicitly forced for every admin).
    - New GET/POST /api/v1/auth/settings (admin-gated) exposing require_admin_totp;
      the value persists to /etc/secubox/auth-runtime.json.
    - Login enforcement gated on _get_require_admin_totp(): runtime file >
      [auth] config > default False. Fail-open: a config read error no longer
      locks admins out.
    - Tests realigned to the reachability-first contract + /settings roundtrip;
      fixture isolates config + runtime paths (10 passing).

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 11 Jul 2026 12:00:00 +0200

secubox-auth (1.0.3-1~bookworm1) bookworm; urgency=medium

  * fix: login failed and /status 500d with NameError: the sessions file was
    defined as __SESSIONS_FILE (dunder) but every handler referenced
    _SESSIONS_FILE (single underscore). Normalised to _SESSIONS_FILE so session
    persistence + status work; logins succeed again.

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 11 Jul 2026 08:00:00 +0200

secubox-auth (1.0.2-1~bookworm2) bookworm; urgency=medium

  * webui: hybrid-dark cyan reskin (WebUI Panel Guidelines) baked into the package.

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 10 Jul 2026 10:00:00 +0200

secubox-auth (1.0.2-1~bookworm1) bookworm; urgency=medium

  * Fix (#498) : ntp_health.probe() falls back to timedatectl/systemd-
    timesyncd when chronyc isn't installed. SecuBox boxes ship with
    timesyncd by default, so the chrony-only probe always returned
    `synced: False` and the admin login UI permanently showed
    'Clock not synced' (TOTP window then widened to ±60s but the
    operator was still confused). Added a chrony → timedatectl
    fallback chain with explicit `source` in the response so the
    operator can see which backend answered.

 -- Gérald Kerma <devel@cybermind.fr>  lun., 08 juin 2026 07:49:50 +0000

secubox-auth (1.0.4-1~bookworm1) bookworm; urgency=medium

  * Add dynamic menu system with menu.d JSON definitions
  * Menu items auto-registered when package installed

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 21 Mar 2026 16:36:21 +0100

secubox-auth (1.0.3-1~bookworm1) bookworm; urgency=medium

  * Add UMask=0000 for world-accessible sockets
  * Add www-data to secubox group for nginx access
  * Make dashboard endpoints public (no auth for read-only)
  * Fix security endpoint error handling

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 21 Mar 2026 08:12:37 +0100

secubox-auth (1.0.2-1~bookworm1) bookworm; urgency=medium

  * Fix RuntimeDirectory conflict between services
  * Only secubox-core manages /run/secubox directory
  * Fix secubox-core.service install path (use /usr/lib/systemd/system)

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 21 Mar 2026 08:06:00 +0100

secubox-auth (1.0.1-1~bookworm1) bookworm; urgency=medium

  * Initial release — migration depuis SecuBox OpenWrt.
  * Port de luci-app-auth-guardian : backend FastAPI + frontend conservé.

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 21 Mar 2026 07:36:46 +0100
