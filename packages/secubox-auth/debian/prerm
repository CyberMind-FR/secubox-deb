#!/bin/bash
set -e
case "$1" in
  remove|upgrade)
    systemctl stop    secubox-auth.service 2>/dev/null || true
    systemctl disable secubox-auth.service 2>/dev/null || true
    ;;
esac
#DEBHELPER#
