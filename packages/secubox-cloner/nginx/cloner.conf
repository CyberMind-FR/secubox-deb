location /api/v1/cloner/ { proxy_pass http://unix:/run/secubox/cloner.sock; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; }
location /cloner/ { alias /usr/share/secubox/www/cloner/; try_files $uri $uri/ /cloner/index.html; }
