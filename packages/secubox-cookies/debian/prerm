#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        systemctl stop secubox-cookies.service || true
        systemctl disable secubox-cookies.service || true
        ;;
esac

#DEBHELPER#

exit 0
