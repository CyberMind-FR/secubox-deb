# cookies API
location /api/v1/cookies/ {
    proxy_pass http://unix:/run/secubox/aggregator.sock:/api/v1/cookies/;
    include /etc/nginx/snippets/secubox-proxy.conf;
    proxy_intercept_errors on;
}
