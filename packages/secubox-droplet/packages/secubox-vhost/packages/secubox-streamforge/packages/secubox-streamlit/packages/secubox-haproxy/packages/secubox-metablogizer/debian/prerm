#!/bin/bash
set -e
case "$1" in
  remove|upgrade|deconfigure)
    systemctl stop secubox-metablogizer.service || true
    systemctl disable secubox-metablogizer.service || true
    ;;
esac
#DEBHELPER#
