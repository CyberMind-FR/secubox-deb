#!/bin/bash
set -e

case "$1" in
    remove|upgrade)
        systemctl stop secubox-eye-remote.service || true
        systemctl disable secubox-eye-remote.service || true
        ;;
esac

#DEBHELPER#

exit 0
