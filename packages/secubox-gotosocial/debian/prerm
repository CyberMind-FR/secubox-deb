#!/bin/bash
set -e
case "$1" in
  remove|upgrade)
    # Remove nginx location config
    rm -f /etc/nginx/secubox.d/gotosocial.conf
    # Stop and disable service
    systemctl stop    secubox-gotosocial.service 2>/dev/null || true
    systemctl disable secubox-gotosocial.service 2>/dev/null || true
    # Reload nginx to remove location
    systemctl reload nginx 2>/dev/null || true
    ;;
esac
#DEBHELPER#
