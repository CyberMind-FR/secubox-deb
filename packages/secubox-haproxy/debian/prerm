#!/bin/bash
set -e
case "$1" in
  remove|upgrade|deconfigure)
    rm -f /etc/nginx/secubox.d/haproxy.conf
    systemctl stop secubox-haproxy.service || true
    systemctl disable secubox-haproxy.service || true
    systemctl reload nginx 2>/dev/null || true
    ;;
esac
#DEBHELPER#
