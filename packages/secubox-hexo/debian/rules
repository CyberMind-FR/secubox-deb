#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_install:
	# API files
	install -d debian/secubox-hexo/usr/lib/secubox/hexo/
	cp -r api debian/secubox-hexo/usr/lib/secubox/hexo/
	# Static www files
	install -d debian/secubox-hexo/usr/share/secubox/www
	[ -d www ] && cp -r www/. debian/secubox-hexo/usr/share/secubox/www/ || true
	# Menu definitions
	install -d debian/secubox-hexo/usr/share/secubox/menu.d
	[ -d menu.d ] && cp -r menu.d/. debian/secubox-hexo/usr/share/secubox/menu.d/ || true
	# Modular nginx config
	install -d debian/secubox-hexo/etc/nginx/secubox.d
	[ -f nginx/hexo.conf ] && cp nginx/hexo.conf debian/secubox-hexo/etc/nginx/secubox.d/ || true
