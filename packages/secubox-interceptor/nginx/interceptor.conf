# interceptor API
location /api/v1/interceptor/ {
    proxy_pass http://unix:/run/secubox/aggregator.sock:/api/v1/interceptor/;
    include /etc/nginx/snippets/secubox-proxy.conf;
    proxy_intercept_errors on;
}
