# ipblock API
location /api/v1/ipblock/ {
    proxy_pass http://unix:/run/secubox/aggregator.sock:/api/v1/ipblock/;
    include /etc/nginx/snippets/secubox-proxy.conf;
    proxy_intercept_errors on;
}
