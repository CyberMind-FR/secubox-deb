# debian/secubox.yaml
name: secubox-jellyfin
category: media
tier: pro
description: "Jellyfin media server in a Debian LXC (partner library auto-wire)"

depends:
  - secubox-core

api:
  socket: /run/secubox/jellyfin.sock
  health: /api/v1/jellyfin/health

ui:
  path: /jellyfin/
