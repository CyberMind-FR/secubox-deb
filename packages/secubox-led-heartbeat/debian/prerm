#!/bin/bash
set -e

case "$1" in
    remove|upgrade|deconfigure)
        # Stop service and turn off LEDs
        systemctl stop secubox-led-heartbeat.service 2>/dev/null || true
        ;;
esac

#DEBHELPER#

exit 0
