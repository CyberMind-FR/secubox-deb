#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    systemctl daemon-reload
    systemctl enable secubox-localrecall.service || true
    systemctl start secubox-localrecall.service || true
fi
#DEBHELPER#
exit 0
