#!/bin/sh
set -e
# No dedicated systemd unit is shipped (aggregator serves in-process), so
# there is nothing to stop/disable here. Kept for dpkg maintainer-script
# symmetry and future use.
#DEBHELPER#
exit 0
