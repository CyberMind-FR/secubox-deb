secubox-matrix (1.1.2-2) bookworm; urgency=medium

  * UNITE VESTIGE RETIREE (#1362). Ce module etait livre avec une unite
    systemd individuelle, `secubox-matrix.service`, qui n'a JAMAIS demarre : son
    `systemctl start` echouait a chaque installation, masque par un
    `|| true`. Il est en realite servi deux fois autrement — monte en
    processus par l'agregateur, qui le liste dans aggregator.toml, et son
    socket tenu par secubox-groupd depuis sa declaration groupable-root.d.
    L'unite etait un reste d'avant ce regroupement.
  .
    VERIFIE AVANT RETRAIT, et c'est ce qui autorise le geste : aucune
    `location` nginx ne pointe vers /run/secubox/matrix.sock — tout passe
    par aggregator.sock — et groupd lit ses declarations, jamais les
    fichiers .service. Le module continue donc de repondre a l'identique.
  .
    Le postinst retire en plus le lien d'activation laisse dans
    /etc/systemd/system par un `enable` d'une version anterieure : dpkg ne
    le connait pas et ne l'aurait pas efface, et systemd aurait continue
    d'essayer au demarrage une unite dont le fichier a disparu.

 -- Gerald Kerma <devel@cybermind.fr>  Tue, 15 Sep 2026 18:22:19 +0200

secubox-matrix (1.1.1-2) bookworm; urgency=medium

  * L'unite retire sa socket perimee via `ExecStartPre=+/bin/rm`.
    /run/secubox porte le bit collant : on n'y delie que ce qu'on possede,
    et une socket laissee par un autre utilisateur rend le demarrage
    impossible — le service echoue, redemarre, echoue encore, en silence.
  * `RuntimeDirectory=secubox` retire : systemd efface le contenu d'un
    RuntimeDirectory au redemarrage, et ce repertoire est partage par ~95
    unites. Une seule qui le declare emporte les sockets des autres.

 -- Gerald KERMA <devel@cybermind.fr>  Sun, 16 Aug 2026 10:35:59 +0200

secubox-matrix (1.1.0-2) bookworm; urgency=medium

  * Phase 2: Requires=secubox-core.service -> Wants= on this module's unit
    (remove the hard cascade-stop when secubox-core restarts/fails).

 -- Gerald KERMA <devel@cybermind.fr>  Sun, 19 Jul 2026 12:00:00 +0200

secubox-matrix (1.0.0-2) bookworm; urgency=medium

  * webui: hybrid-dark cyan reskin (WebUI Panel Guidelines) baked into the package.

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 10 Jul 2026 10:00:00 +0200

secubox-matrix (1.0.0-1) bookworm; urgency=medium

  * Initial release
  * LXC-based Matrix Synapse homeserver
  * User and room management
  * Bridge support (Telegram, Discord, Signal)
  * Element web client integration
  * Backup/restore functionality

 -- Gandalf / CyberMind <gk@cybermind.fr>  Fri, 28 Mar 2026 09:00:00 +0100
