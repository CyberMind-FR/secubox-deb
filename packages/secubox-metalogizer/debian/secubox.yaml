# debian/secubox.yaml
# Auto-generated from debian/control

name: secubox-metalogizer
category: misc
tier: lite
description: "SecuBox Metalogizer - Log processor and analyzer"

depends:
  - secubox-core

api:
  socket: /run/secubox/metalogizer.sock
  health: /api/v1/metalogizer/health

ui:
  path: /srv/secubox/www/metalogizer
