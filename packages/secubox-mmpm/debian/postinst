#!/bin/bash
set -e
case "$1" in
  configure)
    install -d -o secubox -g secubox -m 750 /var/lib/secubox/mmpm
    systemctl daemon-reload
    systemctl enable secubox-mmpm.service
    systemctl start secubox-mmpm.service || true
    systemctl reload nginx 2>/dev/null || true
    ;;
esac
#DEBHELPER#
