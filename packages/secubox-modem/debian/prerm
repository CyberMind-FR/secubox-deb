#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        systemctl stop secubox-modem.service || true
        systemctl disable secubox-modem.service || true
        ;;
esac

#DEBHELPER#

exit 0
