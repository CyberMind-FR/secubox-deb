#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    mkdir -p /var/lib/secubox/ndpid
    chown -R secubox:secubox /var/lib/secubox/ndpid

    systemctl daemon-reload
    systemctl enable secubox-ndpid.service || true
    systemctl start secubox-ndpid.service || true
fi
#DEBHELPER#
exit 0
