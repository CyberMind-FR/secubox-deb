#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_install:
	install -d debian/secubox-netdata/usr/lib/secubox/netdata/
	cp -r api debian/secubox-netdata/usr/lib/secubox/netdata/
	install -d debian/secubox-netdata/usr/share/secubox/www
	[ -d www ] && cp -r www/. debian/secubox-netdata/usr/share/secubox/www/ || true
	install -d debian/secubox-netdata/usr/share/secubox/menu.d
	[ -d menu.d ] && cp -r menu.d/. debian/secubox-netdata/usr/share/secubox/menu.d/ || true
	# Modular nginx config
	install -d debian/secubox-netdata/etc/nginx/secubox.d
	[ -f nginx/netdata.conf ] && cp nginx/netdata.conf debian/secubox-netdata/etc/nginx/secubox.d/ || true
