# netdiag API
location /api/v1/netdiag/ {
    proxy_pass http://unix:/run/secubox/aggregator.sock:/api/v1/netdiag/;
    include /etc/nginx/snippets/secubox-proxy.conf;
    proxy_intercept_errors on;
}
