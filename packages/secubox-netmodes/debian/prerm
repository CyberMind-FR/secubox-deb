#!/bin/bash
set -e
case "$1" in
  remove|upgrade)
    systemctl stop    secubox-netmodes.service 2>/dev/null || true
    systemctl disable secubox-netmodes.service 2>/dev/null || true
    ;;
esac
#DEBHELPER#
