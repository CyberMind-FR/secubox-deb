#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    systemctl daemon-reload
    systemctl enable secubox-network-anomaly.service || true
    systemctl start secubox-network-anomaly.service || true
fi
#DEBHELPER#
exit 0
