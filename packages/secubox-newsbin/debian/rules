#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_install:
	# API files
	install -d debian/secubox-newsbin/usr/lib/secubox/newsbin/
	cp -r api debian/secubox-newsbin/usr/lib/secubox/newsbin/
	# Static www files
	install -d debian/secubox-newsbin/usr/share/secubox/www
	[ -d www ] && cp -r www/. debian/secubox-newsbin/usr/share/secubox/www/ || true
	# Menu definitions
	install -d debian/secubox-newsbin/usr/share/secubox/menu.d
	[ -d menu.d ] && cp -r menu.d/. debian/secubox-newsbin/usr/share/secubox/menu.d/ || true
	# Modular nginx config
	install -d debian/secubox-newsbin/etc/nginx/secubox.d
	[ -f nginx/newsbin.conf ] && cp nginx/newsbin.conf debian/secubox-newsbin/etc/nginx/secubox.d/ || true
