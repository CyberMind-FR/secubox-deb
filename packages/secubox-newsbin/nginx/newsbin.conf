location /api/v1/newsbin/ { proxy_pass http://unix:/run/secubox/newsbin.sock; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; }
location /newsbin/ { alias /usr/share/secubox/www/newsbin/; try_files $uri $uri/ /newsbin/index.html; }
