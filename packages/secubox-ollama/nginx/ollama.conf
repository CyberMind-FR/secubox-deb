# /etc/nginx/secubox.d/ollama.conf
# Installed by secubox-ollama — auto-registered on install, removed on purge
location /api/v1/ollama/ {
    proxy_pass http://unix:/run/secubox/ollama.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
