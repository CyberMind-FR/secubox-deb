secubox-ossec (1.1.1-1~bookworm1) bookworm; urgency=high

  * GARDE JWT SUR TOUTE L'API (#1256, lot P1). MODULE-COMPLIANCE.md impose
    « All endpoints (except /health) MUST use JWT authentication » ; ce
    module ne l'appliquait nulle part.
  * 15 routes sur 16 etaient ouvertes. Meme remarque que pour wazuh : l'API de
    gestion d'un HIDS anonyme est un cadeau fait a l'attaquant.
  * Et rien ne rattrapait l'oubli en amont : l'aggregator se contente de
    app.mount() sans middleware, le snippet nginx secubox-proxy.conf TRANSMET
    l'en-tete Authorization sans jamais le verifier, et auth_request
    /__sbx_auth_verify teste l'appartenance au LAN, pas un jeton.
  * dependencies=[Depends(require_jwt)] plutot qu'un parametre user=Depends() :
    la garde porte sur la route, aucun corps de fonction n'est touche.
  * /health reste public (sonde systemd/nginx). Verifie a l'execution : toute
    autre route rend 401 sans jeton. Couvert par tests/test_conformite_jwt.py.

 -- Gerald Kerma <devel@cybermind.fr>  Thu, 10 Sep 2026 19:20:00 +0200

secubox-ossec (1.1.0-1~bookworm1) bookworm; urgency=medium

  * Installer la route nginx AUSSI dans /etc/nginx/secubox-routes.d/ : c'est
    le seul repertoire que le vhost principal inclut. Livree dans secubox.d/
    uniquement, la route n'etait JAMAIS lue, et il fallait la recopier a la
    main pour que le module fonctionne.
  * Source du depot realignee sur la version VIVANTE, a l'octet pres : les
    deux avaient diverge. (ref #989)

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 06 Aug 2026 07:40:00 +0200

secubox-ossec (1.0.0-2) bookworm; urgency=medium

  * webui: hybrid-dark cyan reskin (WebUI Panel Guidelines) baked into the package.

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 10 Jul 2026 10:00:00 +0200

secubox-ossec (1.0.0-1) stable; urgency=low

  * Initial release
  * OSSEC service control
  * Alert viewing and statistics
  * File integrity monitoring (syscheck)
  * Rootkit detection (rootcheck)

 -- SecuBox Team <team@secubox.local>  Sat, 28 Mar 2026 12:30:00 +0000
