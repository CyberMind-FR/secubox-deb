location /api/v1/ossec/ { proxy_pass http://unix:/run/secubox/ossec.sock; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; }
location /ossec/ { alias /usr/share/secubox/www/ossec/; try_files $uri $uri/ /ossec/index.html; }
