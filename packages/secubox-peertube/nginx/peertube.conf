# /etc/nginx/secubox.d/peertube.conf
# Installed by secubox-peertube -- auto-registered on install, removed on purge
location /api/v1/peertube/ {
    proxy_pass http://unix:/run/secubox/peertube.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
