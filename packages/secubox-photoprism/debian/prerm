#!/bin/bash
set -e
case "$1" in
  remove|upgrade)
    # Remove nginx location config
    rm -f /etc/nginx/secubox.d/photoprism.conf
    # Stop and disable service
    systemctl stop    secubox-photoprism.service 2>/dev/null || true
    systemctl disable secubox-photoprism.service 2>/dev/null || true
    # Reload nginx to remove location
    systemctl reload nginx 2>/dev/null || true
    ;;
esac
#DEBHELPER#
