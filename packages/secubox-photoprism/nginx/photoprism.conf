# /etc/nginx/secubox.d/photoprism.conf
# Installed by secubox-photoprism — auto-registered on install, removed on purge
location /api/v1/photoprism/ {
    proxy_pass http://unix:/run/secubox/photoprism.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
