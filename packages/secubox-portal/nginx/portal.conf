# /etc/nginx/secubox.d/portal.conf
# Installed by secubox-portal package
location /api/v1/portal/ {
    proxy_pass http://unix:/run/secubox/portal.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
