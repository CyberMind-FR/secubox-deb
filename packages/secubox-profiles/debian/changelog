secubox-profiles (0.19.8-1~bookworm1) bookworm; urgency=medium

  * fmrelay retiré des profils full / media-lab et du cycle de vie (ref #1485).

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 26 Sep 2026 06:01:26 +0200

secubox-profiles (0.19.7-1~bookworm1) bookworm; urgency=medium

  * Verrous de maintien (ref #1458) : /run/secubox/sleeper-hold/<id>, frais
    (15 min), empêche le sleeper d'endormir un module où un outil travaille.

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 25 Sep 2026 17:12:17 +0200

secubox-profiles (0.19.6-1~bookworm1) bookworm; urgency=medium

  * Politique de cycle de vie : `sentinelle-gsm` en always-on (#1323). C'est un
    capteur de SÉCURITÉ — la règle que ce fichier énonce déjà — et sa route
    passe par son propre socket, l'agrégateur ne déclenchant pas son démarrage.

 -- Gerald KERMA <devel@cybermind.fr>  Tue, 22 Sep 2026 17:54:51 +0200

secubox-profiles (0.19.5-1~bookworm1) bookworm; urgency=medium

  * Politique de cycle de vie : `metablogizer` et `podcaster` passent en
    always-on (#1322). Le waker ne réveille que sur les vhosts de sites ; un
    module dont nginx route `/api/v1/<mod>/` vers SON propre backend (et non
    `aggregator.sock`) rend donc 502 dans le panneau dès que le sleeper l'a
    endormi, et rien ne le relève. Constaté sur metablogizer : trois rechutes
    en une matinée, unité `inactive` + `disabled` sans trace au journal (le
    journal de gk2 ne retient que quelques minutes), et le `POST /webhook` des
    déploiements GitHub muet avec elle.
  * postinst : sur upgrade, le sleeper est RELANCÉ s'il était activé, au lieu
    d'un `try-restart` que le snippet dh rendait inopérant (il stoppe l'unité
    avant, si bien que `try-restart` la trouvait inactive et ne faisait rien).
    Un opérateur ayant activé le sleeper le perdait à chaque mise à jour, sans
    un mot — constaté sur gk2 : 5 jours d'uptime, disparus après un `dpkg -i`.
    `enabled` reste le témoin de l'opt-in, donc une image neuve (--no-enable)
    ne démarre toujours rien.
  * Deux tests sur la politique LIVRÉE : vocabulaire borné (une valeur hors
    vocabulaire retomberait silencieusement sur always-on, l'intention écrite
    serait perdue) et non-régression du critère ci-dessus.

 -- Gerald KERMA <devel@cybermind.fr>  Tue, 22 Sep 2026 14:54:58 +0200

secubox-profiles (0.19.3-1~bookworm2) bookworm; urgency=medium

  * to_toml emet enfin lifecycle et wake_class. Ils n'etaient JAMAIS ecrits :
    un manifeste porte a « on-demand » revenait « always-on » au premier
    passage, en silence — alors que la docstring promettait un aller-retour
    garanti avec load_manifest. Consequence mesuree sur une image neuve : 86
    manifestes derives, pas un avec une ligne lifecycle, donc un sleeper
    actif qui n'avait rien a endormir.
  * Politique de cycle de vie livree (usr/share/secubox/lifecycle-defaults.toml,
    remplacable par /etc/secubox/). 83 modules : 70 on-demand, 7 manual,
    5 always-on, 1 eager. Ces valeurs ne sont pas inventees — ce sont celles
    de gk2, accumulees module par module, et elles ne se deduisent d'AUCUN
    champ : aucun manifeste ne porte de portal_domain, et 66 des 70
    on-demand n'ont aucune route WAF. C'est du jugement operationnel qui
    n'existait que sur une machine, sans qu'aucun paquet ne le possede.
  * scan applique cette politique a la DERIVATION seulement. Un manifeste
    existant fait toujours autorite (pas d'ecrasement sans --force), et un
    module absent de la politique garde always-on : ne rien savoir ne
    justifie jamais d'endormir. Une politique absente ou illisible rend {},
    ce qui laisse tout eveille. (#1308)

 -- Gerald Kerma <devel@cybermind.fr>  Sat, 19 Sep 2026 21:43:23 +0200

