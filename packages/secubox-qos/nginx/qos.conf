# /etc/nginx/secubox.d/qos.conf
# Installed by secubox-qos package
location /api/v1/qos/ {
    proxy_pass http://unix:/run/secubox/aggregator.sock:/api/v1/qos/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
