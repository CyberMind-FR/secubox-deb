# reporter API
location /api/v1/reporter/ {
    proxy_pass http://unix:/run/secubox/aggregator.sock:/api/v1/reporter/;
    include /etc/nginx/snippets/secubox-proxy.conf;
    proxy_intercept_errors on;
}
