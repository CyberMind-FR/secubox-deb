location /api/v1/rezapp/ { proxy_pass http://unix:/run/secubox/rezapp.sock; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; }
location /rezapp/ { alias /usr/share/secubox/www/rezapp/; try_files $uri $uri/ /rezapp/index.html; }
