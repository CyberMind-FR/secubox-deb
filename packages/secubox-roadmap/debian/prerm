#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        systemctl stop secubox-roadmap.service || true
        systemctl disable secubox-roadmap.service || true
        ;;
esac

#DEBHELPER#

exit 0
