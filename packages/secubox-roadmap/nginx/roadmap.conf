# roadmap API
location /api/v1/roadmap/ {
    proxy_pass http://unix:/run/secubox/aggregator.sock:/api/v1/roadmap/;
    include /etc/nginx/snippets/secubox-proxy.conf;
    proxy_intercept_errors on;
}
