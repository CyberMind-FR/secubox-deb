# /etc/nginx/secubox.d/routes.conf
# Installed by secubox-routes package
location /api/v1/routes/ {
    proxy_pass http://unix:/run/secubox/routes.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
