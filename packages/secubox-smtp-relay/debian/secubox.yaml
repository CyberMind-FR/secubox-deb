# debian/secubox.yaml
# Auto-generated from debian/control

name: secubox-smtp-relay
category: email
tier: lite
description: "SecuBox SMTP Relay — Email forwarding through smarthost"

depends:
  - secubox-core

api:
  socket: /run/secubox/smtp-relay.sock
  health: /api/v1/smtp-relay/health

ui:
  path: /srv/secubox/www/smtp-relay
