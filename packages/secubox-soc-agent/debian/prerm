#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        systemctl stop secubox-soc-agent.service || true
        systemctl disable secubox-soc-agent.service || true
        ;;
esac

#DEBHELPER#

exit 0
