# debian/secubox.yaml
# Auto-generated from debian/control

name: secubox-soc-agent
category: dashboard
tier: lite
description: "SecuBox SOC Agent — Edge Node Agent for SOC Integration"

depends:
  - secubox-core

api:
  socket: /run/secubox/soc-agent.sock
  health: /api/v1/soc-agent/health
