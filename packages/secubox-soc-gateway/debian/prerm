#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        systemctl stop secubox-soc-gateway.service || true
        systemctl disable secubox-soc-gateway.service || true
        ;;
esac

#DEBHELPER#

exit 0
