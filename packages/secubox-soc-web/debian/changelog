secubox-soc-web (1.1.2-1) bookworm; urgency=medium

  * Replaces/Breaks secubox-hub (+ soc↔soc-web) : www/soc/index.html était livré
    par 3 paquets (soc, soc-web, hub) sans Replaces → dpkg refusait l'upgrade
    (« trying to overwrite … also in secubox-hub »). Débloqué proprement.

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 04 Sep 2026 13:30:00 +0200

secubox-soc-web (1.1.1-1) bookworm; urgency=medium

  * nginx snippet: remove duplicate `location /soc/` block (already defined
    inline in secubox-hub's sites-enabled/webui.conf). The snippet now only
    declares routes UNIQUE to soc-web (the /api/v1/soc-gateway/ proxy).
  * Fixes "duplicate location /soc/" nginx test failure that silently
    blocked every "systemctl reload nginx" on boards where webui.conf
    inline + secubox.d/soc.conf + secubox.d/soc-web.conf overlapped.
  * Operator note: if an orphan /etc/nginx/secubox.d/soc.conf still exists
    on the board from a previous (now-removed) package, it must be cleaned
    manually (no package owns it).
  * Closes: #163

 -- Gerald KERMA <devel@cybermind.fr>  Sun, 17 May 2026 07:26:00 +0200

secubox-soc-web (1.1.0-1) bookworm; urgency=medium

  * Phase 5: Hierarchical Mode UI
  * New features:
    - Global View page for central SOC
    - Cross-region threat visualization
    - Regional SOC status grid
    - Settings page for mode configuration
    - Regional enrollment workflow
    - Token generation for regional SOCs
    - Mode indicator in header and sidebar
  * UI enhancements:
    - Mode-aware navigation (central/regional/edge)
    - Hierarchy status display
    - Cross-region threat table

 -- Gerald KERMA <devel@cybermind.fr>  Tue, 01 Apr 2026 15:30:00 +0200

secubox-soc-web (1.0.0-1) bookworm; urgency=medium

  * Initial release
  * React + Vite web dashboard for SOC
  * Features:
    - Fleet overview with health grid
    - Real-time alert stream via WebSocket
    - Correlated threats table
    - Threat map visualization
    - Node detail with service management
    - Cyberpunk-themed responsive UI

 -- Gerald KERMA <devel@cybermind.fr>  Tue, 01 Apr 2026 15:00:00 +0200
