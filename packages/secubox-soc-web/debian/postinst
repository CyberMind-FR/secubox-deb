#!/bin/sh
set -e

case "$1" in
    configure)
        # Config is in /etc/nginx/secubox.d/ - auto-included by main server block
        # Reload nginx if running
        if systemctl is-active --quiet nginx; then
            systemctl reload nginx || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
