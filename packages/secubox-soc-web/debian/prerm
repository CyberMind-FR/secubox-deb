#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        # Config in /etc/nginx/secubox.d/ removed automatically by dpkg
        # Reload nginx if running
        if systemctl is-active --quiet nginx; then
            systemctl reload nginx || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
