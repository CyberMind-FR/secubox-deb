#!/bin/bash
set -e
case "$1" in
  remove|upgrade|deconfigure)
    rm -f /etc/nginx/secubox.d/streamlit.conf
    systemctl stop secubox-streamlit.service || true
    systemctl disable secubox-streamlit.service || true
    systemctl reload nginx 2>/dev/null || true
    ;;
esac
#DEBHELPER#
