#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        systemctl stop secubox-traffic.service || true
        systemctl disable secubox-traffic.service || true
        ;;
esac

#DEBHELPER#

exit 0
