secubox-users (1.4.3-1~bookworm1) bookworm; urgency=medium

  * webui: per-user "Reset 2FA" button (clears TOTP via /user/<u>/totp/disable),
    shown only for TOTP-enrolled users; and an "Admin 2FA: ON/OFF" toolbar toggle
    wired to /api/v1/auth/settings for the require_admin_totp policy.

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 11 Jul 2026 12:00:00 +0200

secubox-users (1.4.2-1~bookworm2) bookworm; urgency=medium

  * webui: hybrid-dark cyan reskin (WebUI Panel Guidelines) baked into the package.

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 10 Jul 2026 10:00:00 +0200

secubox-users (1.4.2-1~bookworm1) bookworm; urgency=medium

  * Fix (#498) : postinst now also chowns /etc/secubox/auth.toml to
    root:secubox 0640 (only users.json was covered before). Without
    this both files were unreadable by the aggregator user (secubox)
    after Phase 7.D consolidation : user_store.get_user(admin) fell
    through to auth.toml fallback which was ALSO unreadable, and
    every login returned 'Identifiants incorrects' regardless of
    password. Live diagnostic on gk2 surfaced two stacked permission
    denials — auth.toml has never had its perms set by any postinst.

 -- Gérald Kerma <devel@cybermind.fr>  lun., 08 juin 2026 08:09:50 +0000

secubox-users (1.4.1-1~bookworm1) bookworm; urgency=low

  * Rewrite Description to identify this as local-account management
    (Argon2id + TOTP + RBAC) and disambiguate from the five other
    identity-stack packages (closes #385, part of identity+monitoring
    naming sweep)

 -- Gerald KERMA <devel@cybermind.fr>  Wed, 27 May 2026 14:00:00 +0000

secubox-users (1.4.0-1~bookworm1) bookworm; urgency=medium

  * Auth rework (ref #120): users.json v2 schema (argon2id hashes + TOTP block)
  * Migrate-v1-to-v2 script auto-runs in postinst (idempotent + .v1.bak backup)
  * Seed admin + <hostname> users on fresh install, must_change_password=true
  * usersctl rewritten in Python over the new secubox_users.engine module
  * New endpoints: /user/{u}/disable, /enable, /password, /totp/disable,
    /totp/backup-codes, /sessions, /sessions/revoke (RBAC-gated)
  * /import routed through engine.create_user (rejects password_hash injection)
  * Depends: python3-argon2, python3-pyotp, python3-qrcode, python3-jsonschema
  * Align postinst with secubox user/group (fixes fresh-install permissions)

 -- Gerald KERMA <devel@cybermind.fr>  Tue, 13 May 2026 12:00:00 +0200

secubox-users (1.2.0-1~bookworm1) bookworm; urgency=medium

  * Add Role-Based Access Control (RBAC)
  * Add permissions management system
  * Add role creation, editing, and assignment
  * Add user permission checking endpoints
  * Add ACL matrix view
  * Default roles: admin, operator, user, guest
  * 35+ granular permissions across users, roles, groups, services, system

 -- Gerald KERMA <devel@cybermind.fr>  Mon, 24 Mar 2026 09:15:00 +0100

secubox-users (1.1.0-1~bookworm1) bookworm; urgency=medium

  * Add usersctl CLI controller for unified user management
  * Add groups support with permissions
  * Add import/export functionality for bulk user management
  * Add service provisioning to Nextcloud, Gitea, Email, Matrix, etc.
  * Enhance API with validation and group endpoints
  * New modern Catppuccin-styled frontend with modals
  * Add nginx config for frontend serving

 -- Gerald KERMA <devel@cybermind.fr>  Sun, 23 Mar 2026 10:30:00 +0100

secubox-users (1.0.0-1~bookworm1) bookworm; urgency=medium

  * Initial release

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 21 Mar 2026 18:31:10 +0100
