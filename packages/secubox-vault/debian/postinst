#!/bin/bash
set -e
case "$1" in
  configure)
    install -d -o root -g root -m 700 /var/lib/secubox/vault
    systemctl daemon-reload
    systemctl enable secubox-vault.service
    systemctl start secubox-vault.service || true
    systemctl reload nginx 2>/dev/null || true
    ;;
esac
#DEBHELPER#
