location /api/v1/vm/ { proxy_pass http://unix:/run/secubox/vm.sock; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; }
location /vm/ { alias /usr/share/secubox/www/vm/; try_files $uri $uri/ /vm/index.html; }
