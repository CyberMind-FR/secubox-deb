location /api/v1/voip/ { proxy_pass http://unix:/run/secubox/voip.sock; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; }
location /voip/ { alias /usr/share/secubox/www/voip/; try_files $uri $uri/ /voip/index.html; }
