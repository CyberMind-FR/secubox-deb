# /etc/nginx/secubox.d/voip.conf
# Installed by secubox-voip — auto-registered on install, removed on purge
location /api/v1/voip/ {
    proxy_pass http://unix:/run/secubox/voip.sock:/;
    include /etc/nginx/snippets/secubox-proxy.conf;
}
