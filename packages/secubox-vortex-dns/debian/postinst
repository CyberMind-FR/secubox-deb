#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    systemctl daemon-reload
    systemctl enable secubox-vortex-dns.service || true
    systemctl start secubox-vortex-dns.service || true
fi
#DEBHELPER#
exit 0
