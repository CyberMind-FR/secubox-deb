# /etc/sudoers.d/secubox-waf-ng — droits délégués au service sbxwaf.
#
# Le pont de bannissement que ce fichier ouvrait autrefois (un verbe unique
# `decisions add` vers une source de blocage externe) a été retiré de la
# plateforme : cette source n'existe plus. Aucun droit n'est donc délégué ici
# pour l'instant. Le fichier est conservé (sans aucune autorisation) pour
# réserver le chemin drop-in ; le bannissement effectif est désormais porté
# par l'ensemble nft du WAF (cf. /etc/sudoers.d/secubox-waf, verbe `wafctl ban`).
