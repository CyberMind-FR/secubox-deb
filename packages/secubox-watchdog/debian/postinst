#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    systemctl daemon-reload
    systemctl enable secubox-watchdog.service || true
    systemctl start secubox-watchdog.service || true
fi
#DEBHELPER#
exit 0
