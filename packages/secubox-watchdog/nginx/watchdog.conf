# watchdog API
location /api/v1/watchdog/ {
    proxy_pass http://unix:/run/secubox/aggregator.sock:/api/v1/watchdog/;
    include /etc/nginx/snippets/secubox-proxy.conf;
    proxy_intercept_errors on;
}
