# SPDX-License-Identifier: LicenseRef-CMSD-1.0
# Copyright (c) 2026 CyberMind — Gérald Kerma <devel@cybermind.fr>
# Source-Disclosed License — All rights reserved except as expressly granted.
# See LICENCE-CMSD-1.0.md for terms.
#
# hall.gk2.net — surface publique WebOS (bureau numérique souverain, #1175).
# Écoute sur le port nginx partagé 9080 (comme bbs.gk2), fronté par HAProxy TLS.
# Sert la page statique du Hall + relaie l'API registre vers webos.sock
# (indépendant de l'agrégateur). CSP souveraine : aucune origine tierce
# (polices locales, pas de fetch externe).
server {
    listen 9080;
    server_name hall.gk2.net hall.gk2.secubox.in;

    root /usr/share/secubox/www/hall;
    index index.html;

    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; font-src 'self'; img-src 'self' data:; connect-src 'self'; worker-src 'self' blob:; frame-src 'self' https://admin.gk2.secubox.in https://bbs.gk2.secubox.in https://radio.gk2.secubox.in https://peertube.gk2.secubox.in https://podcaster.gk2.secubox.in https://nc.gk2.secubox.in https://photoprism.gk2.secubox.in https://metanews.gk2.secubox.in https://social.gk2.secubox.in https://socialrelay.gk2.secubox.in https://webmail.gk2.secubox.in https://kbin.gk2.secubox.in; frame-ancestors 'none'; base-uri 'self'" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "same-origin" always;

    location = / { try_files /index.html =404; }

    location /fonts/ {
        alias /usr/share/secubox/www/hall/fonts/;
        expires 30d;
        access_log off;
        try_files $uri =404;
    }

    # Registre normalisé — socket dédié, jamais l'agrégateur.
    location /api/v1/webos/ {
        proxy_pass http://unix:/run/secubox/webos.sock:/api/v1/webos/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_intercept_errors on;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
