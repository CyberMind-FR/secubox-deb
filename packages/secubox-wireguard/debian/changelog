secubox-wireguard (1.1.3-1~bookworm1) bookworm; urgency=medium

  * LECTURE GARDEE (#1256). Les routes d'affichage de ce module — status,
    components, access, stats — repondaient sans authentification : c'etait le
    motif « three-fold » du parc, un choix assume, mais une lecture publique
    reste de la reconnaissance offerte. Elles passent sous
    `require_lecture` : jeton, OU mode tableau de bord depuis le LAN si
    l'operateur l'a arme dans /etc/secubox/secubox.conf.
  * Le defaut est FERME : sans `[tableau_de_bord] actif = true`, ces routes
    exigent un jeton, y compris depuis le LAN.
  * Les sondes de sante restent publiques. Les routes deja sous require_jwt ne
    changent pas — le mode ne les ouvre jamais.

 -- Gerald Kerma <devel@cybermind.fr>  Thu, 10 Sep 2026 23:55:00 +0200

secubox-wireguard (1.1.2-1~bookworm1) bookworm; urgency=high

  * GARDE JWT SUR /peers ET /interfaces (#1261). La liste des pairs d'un VPN
    n'est pas une donnee de tableau de bord : cles publiques, adresses
    autorisees, derniers handshakes, ports d'ecoute — la carte du mesh et de
    qui s'y connecte. L'appelant est le tableau de bord servi par la webui
    admin, ou l'operateur est connecte (require_jwt accepte le cookie).

 -- Gerald Kerma <devel@cybermind.fr>  Thu, 10 Sep 2026 22:10:00 +0200

secubox-wireguard (1.1.1-1~bookworm2) bookworm; urgency=medium

  * L'unite retire sa socket perimee via `ExecStartPre=+/bin/rm`.
    /run/secubox porte le bit collant : on n'y delie que ce qu'on possede,
    et une socket laissee par un autre utilisateur rend le demarrage
    impossible — le service echoue, redemarre, echoue encore, en silence.
  * `RuntimeDirectory=secubox` retire : systemd efface le contenu d'un
    RuntimeDirectory au redemarrage, et ce repertoire est partage par ~95
    unites. Une seule qui le declare emporte les sockets des autres.

 -- Gerald KERMA <devel@cybermind.fr>  Sun, 16 Aug 2026 10:35:59 +0200

secubox-wireguard (1.1.0-1~bookworm2) bookworm; urgency=medium

  * Phase 2: Requires=secubox-core.service -> Wants= on this module's unit
    (remove the hard cascade-stop when secubox-core restarts/fails).

 -- Gerald KERMA <devel@cybermind.fr>  Sun, 19 Jul 2026 12:00:00 +0200

secubox-wireguard (1.0.3-1~bookworm2) bookworm; urgency=medium

  * webui: hybrid-dark cyan reskin (WebUI Panel Guidelines) baked into the package.

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 10 Jul 2026 10:00:00 +0200

secubox-wireguard (1.0.3-1~bookworm1) bookworm; urgency=medium

  * webui: large tunnels (transparent-proxy) now render CONNECTED peers only by
    default (active/recent handshake) with a "Show all" toggle, instead of
    fully suppressing the list — the backend peers call is now fast enough to
    fetch-and-filter. hasChildNodes() loading guard replaces the .peer-list one.

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 10 Jul 2026 08:15:00 +0200

secubox-wireguard (1.0.2-1~bookworm1) bookworm; urgency=medium

  * webui: full rewrite — interface-card dashboard (role-labelled tunnels,
    live status, lazy per-interface peer drawers, up/down + add-peer), certs
    skin, shared sidebar navbar.
  * perf: rewrite wgctl status active-peer count and wgctl peers as single
    awk/`wg show dump` passes (was ~8 subprocesses per peer → minutes and a
    load-average avalanche on the 500+ peer transparent-proxy tunnel).
  * perf: single-flight + short-TTL cache on read-only wgctl endpoints so
    concurrent dashboard polls collapse to one wgctl run.
  * fix: kill the wgctl child on API timeout (asyncio.wait_for left it running
    → zombie CPU pileup); service NoNewPrivileges=false so sudo -n wgctl works.
  * security: fix _parse_wg_show — parse `wg show all dump` by field count and
    never read the interface private key (the sudo route had activated a
    truncated-key disclosure via /stats,/summary,/peers/status); also fixes
    peers being dropped by the old interface/peer line misdetection.
  * webui: interface-list actions via data-attrs + event delegation (no name
    interpolated into inline handlers); peer drawers no longer blink on refresh.
  * api: invalidate the read cache after interface up/down and peer add/remove.

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 10 Jul 2026 07:30:00 +0200

secubox-wireguard (1.0.1-1~bookworm1) bookworm; urgency=medium

  * webui: sudo-route wgctl/wg so interfaces + peers (wg-toolbox/admin/mesh) show
    (wg show dump needs root); sudoers drop-in + visudo.

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 10 Jul 2026 02:00:00 +0200

secubox-wireguard (1.0.0-1~bookworm1) bookworm; urgency=medium

  * Initial release - migration from OpenWrt luci-app-wireguard-dashboard
  * Three-fold architecture (Components/Status/Access)
  * Real wgctl control script with interface and peer management
  * Auto-config generation for new peers
  * QR code generation for mobile clients
  * Migration from OpenWrt with UCI conversion
  * Tabbed UI with peer management

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 22 Mar 2025 15:00:00 +0100
