secubox-wireguard (1.0.2-1~bookworm1) bookworm; urgency=medium

  * webui: full rewrite — interface-card dashboard (role-labelled tunnels,
    live status, lazy per-interface peer drawers, up/down + add-peer), certs
    skin, shared sidebar navbar.
  * perf: rewrite wgctl status active-peer count and wgctl peers as single
    awk/`wg show dump` passes (was ~8 subprocesses per peer → minutes and a
    load-average avalanche on the 500+ peer transparent-proxy tunnel).
  * perf: single-flight + short-TTL cache on read-only wgctl endpoints so
    concurrent dashboard polls collapse to one wgctl run.
  * fix: kill the wgctl child on API timeout (asyncio.wait_for left it running
    → zombie CPU pileup); service NoNewPrivileges=false so sudo -n wgctl works.
  * security: fix _parse_wg_show — parse `wg show all dump` by field count and
    never read the interface private key (the sudo route had activated a
    truncated-key disclosure via /stats,/summary,/peers/status); also fixes
    peers being dropped by the old interface/peer line misdetection.
  * webui: interface-list actions via data-attrs + event delegation (no name
    interpolated into inline handlers); peer drawers no longer blink on refresh.
  * api: invalidate the read cache after interface up/down and peer add/remove.

 -- Gerald KERMA <devel@cybermind.fr>  Fri, 10 Jul 2026 07:30:00 +0200

secubox-wireguard (1.0.1-1~bookworm1) bookworm; urgency=medium

  * webui: sudo-route wgctl/wg so interfaces + peers (wg-toolbox/admin/mesh) show
    (wg show dump needs root); sudoers drop-in + visudo.

 -- Gerald KERMA <devel@cybermind.fr>  Thu, 10 Jul 2026 02:00:00 +0200

secubox-wireguard (1.0.0-1~bookworm1) bookworm; urgency=medium

  * Initial release - migration from OpenWrt luci-app-wireguard-dashboard
  * Three-fold architecture (Components/Status/Access)
  * Real wgctl control script with interface and peer management
  * Auto-config generation for new peers
  * QR code generation for mobile clients
  * Migration from OpenWrt with UCI conversion
  * Tabbed UI with peer management

 -- Gerald KERMA <devel@cybermind.fr>  Sat, 22 Mar 2025 15:00:00 +0100
