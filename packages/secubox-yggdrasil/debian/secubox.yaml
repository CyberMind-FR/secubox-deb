# debian/secubox.yaml
# Auto-generated from debian/control

name: secubox-yggdrasil
category: vpn
tier: lite
description: "SecuBox Mesh DNS - Meshname DNS for mesh networks"

depends:
  - secubox-core

api:
  socket: /run/secubox/mesh.sock
  health: /api/v1/mesh/health

ui:
  path: /srv/secubox/www/mesh
