# CMSD-1.0 license header enrollment allowlist.
# One glob per line (paths relative to repo root).
# **/CONCURRENT_NOTE**: Phase B mega-enroll. This file now matches everything.
# Going forward, new files added to the repo must carry the header — CI enforces it.
# The repo's SKIP_DIRS (kernel-build, redroid, tools/Tow-Boot, output, cache, ...)
# remain excluded by the tool's walker.
**
